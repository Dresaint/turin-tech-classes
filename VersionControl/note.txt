1. Initialization - git 
2. staging:
    (a) git add <filename>  ( to stage one file)
    (b) git add . (to stage all file)

3. commit:
    git commit -m "first commit"

4. push:
    git push -u origin main

        BRANCH
1. make an update from the github = git pull
1. create an branch - git branch branch-name
2. move into the branch = git checkout branch-name
3. to check wthe branches you have = git branch
4. implement a new features or code on the branch
5. stage = git add .
6. commit = git commit -m " comment "
7. push = git push origin branch-name
8. send a pull request as the team lead and merge

        TO DFELETE A BRANCH
    branch -d branch-name

        CLONNING
    To clone a specific branch on GitHub is:
    

